import { RestEndpointMethodTypes } from '@octokit/rest';
export declare type Annotation = NonNullable<NonNullable<RestEndpointMethodTypes['checks']['create']['parameters']['output']>['annotations']>[number];
interface CheckOptions {
    /** The annotations to be posted to the GitHub check */
    annotations: Annotation[];
    /** The number or errors generated during the run */
    errorCount: number;
    /** The number or warnings generated during the run */
    warningCount?: number;
    /** The GitHub app id to create the check with */
    appId: number;
    /** The GitHub private key to create the check with */
    privateKey: string;
    /** The name of the tool thats running */
    tool: string;
    /** The title of the check mark */
    name: string;
    /** Github Enterprise URL */
    githubUrl?: string;
}
/** Add a GitHub check with annotations to the HEAD sha */
export default function createCheck({ annotations, errorCount, warningCount, appId, privateKey, tool, name, githubUrl, }: CheckOptions): Promise<void>;
export {};
//# sourceMappingURL=index.d.ts.map